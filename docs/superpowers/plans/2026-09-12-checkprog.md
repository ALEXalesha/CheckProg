# CheckProg: план реализации

> **Для исполнителей:** выполнять задачи по порядку, отмечая шаги `- [ ]`. План исполняется в этой же сессии (inline), поэтому код не дублируется здесь, а сразу пишется в файлы, указанные в каждой задаче. Спецификация: `docs/superpowers/specs/2026-09-12-checkprog-design.md`.

**Цель:** чек-лист для Windows на Tkinter с portable-сборкой, установщиком и документацией.

**Архитектура:** модель `Checklist` (чистый Python, без tkinter) — единственный источник истины. Окно `App` после каждой операции синхронизирует `ttk.Treeview` с моделью. Пути к настройкам зависят от того, есть ли рядом с exe файл `portable.flag`.

**Стек:** Python 3.13, tkinter/ttk, unittest, PyInstaller 6, Inno Setup 6.

---

### Задача 1: модель `Checklist`

**Файлы:** `checkprog/__init__.py`, `checkprog/model.py`, `tests/test_model.py`

- [ ] Написать тесты: добавление (обрезка краёв, отказ для пустой строки, склейка переносов строк), переключение, переименование, удаление, ошибки индекса, `move` для всех пар (src, dst) с проверкой перестановки, счётчики и прогресс, случайные последовательности операций с проверкой инвариантов, сохранение и загрузка с кириллицей и эмодзи, BOM, битые файлы, атомарность записи (сбой `os.replace` и `json.dump` не портит старый файл и не оставляет временных файлов).
- [ ] Запустить `python -m unittest tests.test_model -v`. Ожидается: ошибка импорта.
- [ ] Реализовать `checkprog/model.py`.
- [ ] Запустить тесты. Ожидается: все проходят.
- [ ] Коммит `feat: checklist model with atomic JSON save`.

### Задача 2: настройки и пути

**Файлы:** `checkprog/settings.py`, `tests/test_settings.py`

- [ ] Тесты: portable-режим с маркером, установленный режим (APPDATA и «Документы» передаются параметрами), чтение настроек из отсутствующего, битого, не-UTF-8 и не-объектного JSON даёт `{}`, запись создаёт папки, запись в недоступное место возвращает `False` без исключения.
- [ ] Запустить, увидеть падение, реализовать, запустить снова.
- [ ] Коммит `feat: portable/installed settings paths`.

### Задача 3: окно приложения

**Файлы:** `checkprog/app.py`, `checkprog/__main__.py`, `tests/test_app.py`

- [ ] Тесты на скрытом окне Tk, диалоги подменены через `unittest.mock`. После каждой операции строки Treeview совпадают с моделью. Добавление через поле ввода; галочка по клику в колонке; редактирование с сохранением (Enter), отменой (Esc) и пустым текстом; удаление с корректным выделением; перемещение Ctrl+↑/↓ и перетаскиванием; заголовок с `*`; сценарии «Сохранить / Не сохранять / Отмена» при закрытии и открытии; открытие битого файла не меняет текущий список; горячие клавиши по keycode (русская раскладка); открытие файла из аргумента командной строки; запоминание последнего файла.
- [ ] Реализовать `app.py`, запустить тесты.
- [ ] Запустить приложение вручную, проверить на скриншоте: галочки видны, строки не обрезаны на высоком DPI, зачёркивание выполненных.
- [ ] Коммит `feat: tkinter checklist window`.

### Задача 4: сборка

**Файлы:** `packaging/launcher.py`, `packaging/installer.iss`, `packaging/build.ps1`

- [ ] `build.ps1`: тесты, затем PyInstaller (`--onefile --windowed`), затем папка `dist\CheckProg-Portable` (exe, `portable.flag`, README) и zip, затем ISCC с `/DAppVersion`.
- [ ] Запустить `powershell -File packaging\build.ps1`. Ожидается: `dist\CheckProg-<версия>-Portable.zip` и `dist\CheckProg-<версия>-Setup.exe`.
- [ ] Проверить собранный exe: в portable-режиме `settings.json` появляется рядом с exe, без маркера — в `%APPDATA%\CheckProg`.
- [ ] Коммит `build: portable zip and Inno Setup installer`.

### Задача 5: документация

**Файлы:** `README.md`

- [ ] Что это, скачивание и запуск portable, установка и удаление, работа со списками, горячие клавиши, формат файла, где хранятся настройки, сборка из исходников, частые проблемы (SmartScreen и антивирус для PyInstaller-exe).
- [ ] Коммит `docs: user guide`.

### Задача 6: проверка на баги

- [ ] Выписать инварианты и гипотезы о багах. Каждую подтвердить или опровергнуть тестом или экспериментом. Найденное исправить с регрессионным тестом.
- [ ] Итоговый прогон тестов и сборки, коммит, `git push -u origin main`.
